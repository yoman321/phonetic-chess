[pytest]
# `.` is the rootdir (backend/), so tests can import controller_operations,
# queries and error_logger the same way application.py does.
pythonpath = .
testpaths = tests
markers =
    integration: needs a live Postgres; see scripts/testdb.sh
